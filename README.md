# Schrodinger
Schrödinger is a command-line Python tool that clones and organizes folder structures based on file extensions — for example, .en and .hu files for translation projects.  The tool copies the directory tree, replicates files matching certain patterns, and preserves data integrity without modifying the originals.
