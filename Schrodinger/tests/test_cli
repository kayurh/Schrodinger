import subprocess
import os

def test_cli_help():
    result = subprocess.run(
        ["python", "-m", "schrodinger.cli", "--help"],
        capture_output=True,
        text=True
    )
    assert "usage" in result.stdout.lower()
